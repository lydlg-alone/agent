# 维护文档

本文档用于说明当前项目的启动、构建、测试、配置维护和常见排查方式。

## 运行环境

- Windows 10/11
- Node.js 18 及以上
- npm 9 及以上

## 项目入口

应用代码目录：

```text
E:\基于Deepseek的智能体学习软件\1
```

## 常用命令

开发启动：

```powershell
cd "E:\基于Deepseek的智能体学习软件\1"
npm run dev
```

前端构建：

```powershell
cd "E:\基于Deepseek的智能体学习软件\1"
npm run build
```

集成测试：

```powershell
cd "E:\基于Deepseek的智能体学习软件\1"
npm test
```

## 依赖说明

新增依赖（最近更新）：

| 包名 | 用途 |
|------|------|
| `marked` | Markdown → HTML 解析（GFM 支持） |
| `highlight.js` | 代码块语法高亮 |
| `katex` | LaTeX 数学公式渲染 |
| `dompurify` | HTML 安全过滤（XSS 防护） |
| `echarts` | 学习仪表盘可视化图表 |

安装命令：

```powershell
cd "E:\基于Deepseek的智能体学习软件\1"
npm install
```

## 主要目录职责

- `electron/`：桌面主进程与 preload
- `renderer/`：Vue 前端界面
- `renderer/src/components/workspace/`：聊天工作区拆分组件
- `renderer/src/stores/`：Pinia 状态（`runtime.js` 运行时配置、`theme.js` 主题切换）
- `renderer/src/styles/theme.css`：CSS 变量体系 + 明暗双主题定义
- `renderer/src/utils/markdownRenderer.js`：Markdown 渲染管线（marked + hljs + katex + DOMPurify）
- `server/src/routes/`：HTTP 路由（含 `analytics.js` 仪表盘数据）
- `server/src/services/`：核心业务逻辑（含 `analyticsService.js` 统计聚合）
- `server/src/db/`：数据库结构
- `server/data/`：SQLite 本地数据文件

## 配置文件维护

运行时 API 配置文件不保存在项目目录，而是保存在当前用户文档目录：

```text
文档\agent API\runtime-config.json
```

维护说明：

- 应用会自动读取系统真实"文档"目录，不依赖写死路径
- Windows 下 `API Key` 使用当前用户上下文保护
- 外部修改配置文件后，前端设置页和聊天页会自动同步

建议：

- 修改配置优先通过应用设置页完成
- 手动编辑配置文件后，确认 JSON 格式合法
- 不要把配置文件提交进 Git 仓库

## 主题配置

主题状态保存在浏览器 `localStorage`（键名 `ai-study-theme-mode`），支持三种模式：

- `light`：亮色主题
- `dark`：暗色主题
- `system`：跟随操作系统偏好（默认）

CSS 变量体系定义在 `renderer/src/styles/theme.css`，所有组件颜色通过 `var(--xxx)` 引用。暗色模式通过 `[data-theme="dark"]` 选择器覆盖变量值。

如需强制重置主题，可在浏览器控制台执行：

```js
localStorage.removeItem("ai-study-theme-mode");
location.reload();
```

## 本地数据库维护

数据库目录：

```text
1/server/data/
```

常见文件：

- `app.db`
- `app.db-shm`
- `app.db-wal`

维护说明：

- `app.db` 保存会话、消息、知识库、文档块、连接历史、学习计划、练习题、答案等数据
- 聊天历史、知识文档和检索块默认都落在本地库中
- 检索依赖 `document_chunks` 和 `document_chunks_fts`
- 仪表盘统计从上述表中实时聚合计算

建议：

- 做版本备份时优先备份整个 `server/data/` 目录
- 如果怀疑索引异常，优先检查文档导入和清空逻辑是否完整执行
- 不要将运行中的 `app.db-wal`、`app.db-shm` 单独拷走替换

## 聊天链路维护

当前聊天页支持：

- 历史会话保存
- 新建对话自动入历史
- 会话重命名、删除、清空
- 附件上传与解析
- 流式输出
- 知识库召回与引用
- Markdown 渲染（表格、代码高亮、LaTeX 公式）

排查顺序建议：

1. 先看设置页的当前 `baseUrl`、`modelId`、`API Key` 是否有效
2. 再看后端 `3001` 是否正常启动
3. 再看聊天是否走普通回复还是流式回复
4. 如果引用缺失，再查知识库导入和检索块索引
5. 如果 Markdown/公式渲染异常，检查 `marked`、`katex`、`highlight.js` 依赖是否已安装

## 知识库维护

当前知识库支持：

- 文档导入
- 列表查询
- 单条删除
- 全量清空
- 聊天自动召回

文档导入说明：

- 文本类内容可直接导入
- PDF、DOCX 会尝试提取文本
- 导入后系统会切块并写入检索表

如果聊天没引用资料，重点检查：

- 文档是否导入成功
- 文档是否生成了 `chunkCount`
- 对应块是否进入 `document_chunks` 和 `document_chunks_fts`

## 仪表盘维护

仪表盘（`/api/analytics/overview`）从 SQLite 各表中实时聚合以下数据：

- 基础计数：会话数、消息数、文档数、练习题数、生成资源数
- 趋势数据：近 7 天每日消息活跃度、近 30 天练习得分趋势
- 分布数据：文档类型分布、最近文档列表

如果仪表盘数据为空或异常：

1. 确认数据库中已有实际数据（发过消息、导入过文档、做过练习）
2. 检查 `analyticsService.js` 中的 SQL 查询是否与当前 schema 兼容
3. 查看后端日志确认 `/api/analytics/overview` 返回 200

## 测试维护

当前最小集成测试位于：

```text
1/server/test/app.integration.test.js
```

覆盖内容：

- 聊天普通回复
- 聊天流式输出
- 知识库导入
- 运行时设置保存与读取
- schema 校验

建议每次改动以下链路后都执行一次：

- 聊天消息接口
- 运行时配置接口
- 知识库导入逻辑
- 会话历史逻辑
- 仪表盘统计逻辑

## 常见问题

### 1. 聊天页没有通过 AI 回复

排查项：

- 设置页是否已保存有效配置
- `文档\agent API\runtime-config.json` 是否存在
- 模型测试是否通过
- 后端能否访问外部模型接口

### 2. 第一次发送消息总是超时

当前聊天请求已适配更长超时，并支持流式输出。若仍出现超时，优先排查：

- 外部模型首响应速度
- 本地网络或代理
- 配置的 `baseUrl` 是否兼容 OpenAI 风格接口

### 3. Markdown 或公式渲染异常

排查项：

- `marked`、`highlight.js`、`katex`、`dompurify` 是否已安装
- 浏览器控制台是否有 `import` 相关报错
- 确认 `markdownRenderer.js` 未被构建工具 tree-shake 移除

### 4. 暗色模式不生效

排查项：

- 检查 `<html>` 标签是否有 `data-theme` 属性
- 检查 `localStorage` 中 `ai-study-theme-mode` 的值
- 确认 `theme.js` store 的 `init()` 已在 `main.js` 中调用

### 5. 仪表盘无数据

排查项：

- 数据库中是否有聊天记录、文档、练习题等数据
- `GET /api/analytics/overview` 是否返回 200
- 查看浏览器 Network 面板确认接口响应内容

### 6. 页面出现乱码

当前项目已增加历史乱码清洗。若仍出现：

- 重启一次后端
- 检查是否为旧数据库残留内容
- 检查是否手动写入了非 UTF-8 文本

### 7. Git 推送失败

如果本机 Git 报 SSL 证书链错误，需要先修本机证书环境。不要把这类临时处理写进项目脚本。

## 发布前检查

发布或推送前建议至少确认：

1. `npm run build` 通过
2. `npm test` 通过
3. 聊天页可以创建新会话并发送消息，Markdown 渲染正常
4. 暗色/亮色主题切换正常
5. 设置页可以读写配置文件
6. 知识库导入后可以在聊天中被召回
7. 仪表盘页面可正常加载并展示图表
