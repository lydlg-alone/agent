# 维护文档

本文档面向后续维护者，说明如何在本机启动、验证、排查和继续开发当前项目。

## 1. 环境要求

- Windows 10/11
- Node.js `>= 18.18.0`
- npm `>= 9`

当前项目实际脚本位于：

```text
F:\AI 学习多智能体工作台\agent-main\1
```

## 2. 安装依赖

```powershell
cd "F:\AI 学习多智能体工作台\agent-main\1"
npm install
```

## 3. 启动项目

推荐启动方式：

```powershell
cd "F:\AI 学习多智能体工作台\agent-main\1"
Remove-Item Env:ELECTRON_RUN_AS_NODE -ErrorAction SilentlyContinue
npm run dev
```

原因：

- 当前 Electron 启动脚本依赖真实 Electron 进程
- 如果系统或终端里残留 `ELECTRON_RUN_AS_NODE=1`，Electron 窗口可能无法正常弹出

`npm run dev` 会同时启动：

- `dev:renderer`：Vite 前端，默认 `5173`
- `dev:server`：Express 后端，默认 `3001`
- `dev:electron`：等待前端和后端端口可用后启动 Electron

## 4. 构建与测试

构建：

```powershell
cd "F:\AI 学习多智能体工作台\agent-main\1"
npm run build
```

测试：

```powershell
cd "F:\AI 学习多智能体工作台\agent-main\1"
npm test
```

当前已补充的集成测试覆盖点：

- 聊天普通回复
- 流式聊天 SSE
- 知识库导入与检索
- 图片附件识别标记
- 运行时配置保存与读取
- 空消息校验

## 5. 关键目录

```text
1/
├─ electron/                         Electron 主进程与 preload
├─ renderer/                         Vue 前端
│  └─ src/
│     ├─ components/workspace/       聊天区、知识库区、状态区组件
│     ├─ services/api.js             前端 API 封装
│     ├─ stores/                     Pinia 状态
│     ├─ styles/theme.css            全局主题变量
│     ├─ utils/markdownRenderer.js   Markdown / 高亮 / LaTeX 渲染
│     └─ views/StudyWorkspaceView.vue
└─ server/
   └─ src/
      ├─ routes/                     HTTP 路由
      ├─ services/                   核心业务逻辑
      ├─ config/database.js          数据库初始化与迁移补丁
      ├─ db/schema.sql               数据库建表脚本
      └─ test/app.integration.test.js
```

## 6. 这次变更后的重点模块

### 6.1 聊天与工具调用

- `server/src/services/chatWorkspaceService.js`

职责：

- 会话与消息持久化
- 组装系统提示词
- 执行知识库检索
- 执行工具调用闭环
- 处理结构化输出
- 处理图片附件视觉输入

### 6.2 混合检索

- `server/src/services/ragService.js`

职责：

- FTS5 关键词召回
- 本地轻量向量构建与相似度计算
- 结果合并
- rerank 重排

### 6.3 网页搜索与 URL 导入

- `server/src/services/webToolService.js`
- `server/src/services/knowledgeBaseService.js`

职责：

- DuckDuckGo HTML 搜索结果抓取
- 网页正文提取
- 网页导入知识库

### 6.4 输入区与开关 UI

- `renderer/src/components/workspace/ComposerPanel.vue`

职责：

- 五个能力开关
- 上传按钮
- 发送按钮
- 输入框自适应高度
- 提示信息布局

## 7. 数据与配置

### 7.1 运行时配置

默认保存在：

```text
文档\agent API\runtime-config.json
```

说明：

- Windows 下 API Key 会做本地保护存储
- 导入导出配置时，优先关注 `provider`、`baseUrl`、`modelId`、`systemPrompt`

### 7.2 SQLite 数据库

默认位于：

```text
1/server/data/app.db
```

本次新增的关键字段：

- `chat_attachments.content_data`
  - 存储图片 data URL，供视觉输入使用
- `document_chunks.embedding_json`
  - 存储本地轻量向量，供向量/混合检索使用

相关表：

- `chat_sessions`
- `chat_messages`
- `chat_attachments`
- `knowledge_documents`
- `document_chunks`
- `document_chunks_fts`

## 8. 常见问题

### 8.1 Electron 窗口起不来

先执行：

```powershell
Remove-Item Env:ELECTRON_RUN_AS_NODE -ErrorAction SilentlyContinue
```

再重新启动 `npm run dev`。

### 8.2 前端能打开，但桌面端没有窗口

检查：

- `5173` 和 `3001` 是否都已启动
- 是否有旧 Electron 进程残留
- 当前终端里是否仍有 `ELECTRON_RUN_AS_NODE`

### 8.3 联网搜索或 URL 导入失败

可能原因：

- 外网不可用
- 目标网站拦截抓取
- 目标网页正文提取后为空

排查建议：

- 先直接在浏览器验证目标 URL 是否能打开
- 再检查后端日志里是否有超时或正文为空错误

### 8.4 图片上传后模型看不懂

先确认：

- 是否开启了 `识图`
- 当前模型是否真的支持视觉输入

说明：

- 代码会把图片作为 `image_url` 多模态消息发送
- 如果模型本身不支持视觉，后端无法强行补齐这项能力

### 8.5 工具/结构化开启后不像普通流式输出

这是当前设计使然：

- 开启 `工具` 或 `结构化` 后，系统需要先完成工具调用或 JSON 整理，再返回最终结果

## 9. 发布前检查

每次较大改动后，至少执行：

1. `npm run build`
2. `npm test`
3. 打开聊天页验证输入框、按钮、五个开关的布局
4. 验证知识库文件导入和 URL 导入
5. 验证混合检索能返回结果
6. 如涉及图片功能，验证图片附件上传和 `识图` 开关
7. 如涉及模型能力，验证一遍运行时配置读写

## 10. 后续继续扩展时的建议

- 若要进一步提升检索质量，可替换为真实 embedding 服务
- 若要增强联网能力，可补充更多搜索源和网页提取策略
- 若要让结构化输出更稳定，可给不同工作流定义固定 schema
- 若要提升视觉体验，可补充前端图片预览和失败提示
