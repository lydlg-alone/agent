# 维护文档

本文档面向后续维护者，说明如何启动、验证、排障和继续开发当前项目。

## 1. 环境要求

- Windows 10/11
- Node.js `>= 18.18.0`
- npm `>= 9`

项目实际代码目录：

```text
F:\AI 学习多智能体工作台\agent-main\1
```

## 2. 安装依赖

```powershell
cd "F:\AI 学习多智能体工作台\agent-main\1"
npm install
```

当前关键依赖包括：

- `electron`
- `vite`
- `better-sqlite3`
- `axios`
- `tesseract.js`

## 3. 启动项目

推荐启动方式：

```powershell
cd "F:\AI 学习多智能体工作台\agent-main\1"
npm run dev
```

说明：

- `npm run dev` 会同时启动：
  - `dev:renderer`
  - `dev:server`
  - `dev:electron`
- `dev:electron` 通过 `scripts/start-electron.cjs` 启动，会自动清理 `ELECTRON_RUN_AS_NODE`。
- 如果你绕过该脚本手动拉起 Electron，再遇到窗口不显示问题，可以手动执行：

```powershell
Remove-Item Env:ELECTRON_RUN_AS_NODE -ErrorAction SilentlyContinue
```

默认端口：

- 前端：`5173`
- 后端：`3001`

## 4. 构建与测试

构建：

```powershell
cd "F:\AI 学习多智能体工作台\agent-main\1"
npm run build
```

测试：

```powershell
cd "F:\AI 学习多智能体工作台\agent-main\1"
npm test
```

当前集成测试已覆盖：

- 无运行时配置时的 mock 聊天回复
- 流式聊天 SSE
- 知识库导入与检索
- 图片附件上传
- 带图片附件继续聊天
- 待发送附件删除
- 运行时配置保存与读取
- 空消息校验

## 5. 关键目录

```text
1/
├─ electron/                         Electron 主进程与 preload
├─ renderer/
│  └─ src/
│     ├─ components/workspace/       聊天区、输入区、附件卡片
│     ├─ services/api.js             前端 API 封装
│     ├─ stores/                     Pinia 状态
│     ├─ styles/                     主题样式
│     ├─ utils/                      格式化与渲染工具
│     └─ views/StudyWorkspaceView.vue
├─ server/
│  ├─ src/
│  │  ├─ config/                     数据库与初始化
│  │  ├─ db/                         schema.sql
│  │  ├─ routes/                     HTTP 路由
│  │  ├─ services/                   核心业务逻辑
│  │  ├─ test/                       集成测试
│  │  └─ utils/                      校验与通用工具
│  └─ data/
├─ scripts/
└─ package.json
```

## 6. 这轮变更后的重点模块

### 6.1 启动修复

文件：

- `scripts/start-electron.cjs`
- `package.json`

说明：

- Electron 启动前自动清理 `ELECTRON_RUN_AS_NODE`
- 避免 Electron 以 Node 模式误启动

### 6.2 聊天主逻辑

文件：

- `server/src/services/chatWorkspaceService.js`

职责：

- 会话与消息持久化
- 工具调用与检索整合
- 图片附件 OCR 摘要接入
- 视觉输入与兼容性降级
- 流式与非流式回复

### 6.3 图片 OCR

文件：

- `server/src/services/imageOcrService.js`

职责：

- 从图片 data URL 解码二进制
- 使用 `tesseract.js` 做 OCR
- 缓存 OCR worker
- 识别失败时自动降级，不阻断聊天

### 6.4 附件交互

文件：

- `renderer/src/components/workspace/ComposerPanel.vue`
- `renderer/src/components/workspace/AttachmentCard.vue`
- `renderer/src/components/workspace/ChatPane.vue`
- `renderer/src/views/StudyWorkspaceView.vue`

职责：

- 输入区附件卡片展示
- 右上角圆形删除按钮
- 聊天气泡附件卡片展示
- 会话重命名弹窗

## 7. 数据与配置

### 7.1 运行时配置

默认路径：

```text
%USERPROFILE%\Documents\agent API\runtime-config.json
```

可通过环境变量覆盖：

- `AGENT_CONFIG_DIR`

说明：

- Windows 下 API Key 会以当前用户 DPAPI 方式保护存储。

### 7.2 SQLite 数据库

默认路径：

```text
1/server/data/app.db
```

可通过环境变量覆盖：

- `AGENT_DATA_DIR`

### 7.3 OCR 缓存

默认路径：

```text
1/data/ocr-cache
```

如设置 `AGENT_DATA_DIR`，OCR 缓存会随之迁移。

### 7.4 与本轮能力相关的关键字段

- `chat_attachments.content_data`
  - 保存图片 data URL
- `chat_attachments.content_excerpt`
  - 保存 OCR 摘要或附件文本摘要
- `document_chunks.embedding_json`
  - 保存本地轻量向量

## 8. 常见问题

### 8.1 `npm run dev` 后窗口没出来

检查：

- `5173` 和 `3001` 是否都已启动
- 是否存在旧 Electron 进程残留
- 是否绕开了 `scripts/start-electron.cjs`

### 8.2 上传图片后模型报 `image_url` 相关错误

当前版本已在后端做兼容处理。若仍出现，优先检查：

- 是否运行的是最新代码
- 后端是否已重启
- 当前模型配置是否指向与界面显示一致的供应商和模型

### 8.3 图片上传成功，但没有识别到文字

可能原因：

- 图片本身文字太少或不清晰
- 首次 OCR 需要语言数据，但当前环境无法联网
- 本机未通过 `TESSERACT_LANG_PATH` 提供本地训练数据

说明：

- OCR 失败不会影响附件上传和聊天发送。
- 没识别出文字时，系统仍会保留图片附件并继续正常对话。

### 8.4 点击“重命名”无反应

当前版本已改为 Element Plus 弹窗输入，不再依赖 `window.prompt`。如果又失效，优先检查：

- `StudyWorkspaceView.vue` 是否被回退
- `element-plus` 是否安装完整

## 9. 发布前检查

每次较大改动后，至少执行：

1. `npm run build`
2. `npm test`
3. 验证聊天输入、附件上传、附件删除、重命名会话
4. 验证知识库文件导入与 URL 导入
5. 验证图片附件不会触发 `image_url` 兼容性报错
6. 如环境允许，验证 OCR 能识别一张包含清晰中文文字的图片

## 10. 后续扩展建议

- 为 OCR 增加本地训练数据打包方案，减少首次联网依赖
- 按模型能力做更细的视觉特性白名单
- 为结构化输出补充业务级 schema
- 为附件卡片增加图片缩略图预览
