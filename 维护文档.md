# AI 学习多智能体工作台维护文档

## 1. 文档目的

本文档用于说明 `1` 目录项目的日常维护方式，包括环境准备、启动流程、目录职责、数据维护、故障排查和版本演进注意事项。

## 2. 关联文档

- 项目说明：[`README.md`](./README.md)
- API 文档：[`API文档.md`](./API文档.md)
- 技术说明：[`详细技术说明文档.md`](./详细技术说明文档.md)

## 3. 环境要求

建议环境：

- Node.js 20+
- npm 10+
- Windows 10/11（macOS / Linux 亦可运行服务端和前端）

## 4. 安装与启动

在项目目录 `1` 下执行：

```bash
npm install
npm run dev
```

如果需要单独调试：

```bash
npm run dev:renderer    # Vite 前端，端口 5173
npm run dev:server      # Express API，端口 3001
npm run dev:electron    # Electron 窗口（需等前两个就绪）
```

构建前端：

```bash
npm run build
```

当前构建产物输出到：

```text
1/dist/renderer/
```

## 5. 关键目录维护说明

### 5.1 `1/electron/`

职责：

- 管理 Electron 窗口生命周期
- 开发模式加载 Vite 地址（`http://127.0.0.1:5173`）
- 生产模式加载 `dist/renderer/index.html`
- 通过 preload 暴露有限桌面能力

维护注意：

- 主进程保持精简，不要把业务逻辑堆在 `main.cjs`
- 新增桌面系统能力时，优先经 `preload.cjs` 暴露

### 5.2 `1/renderer/`

职责：

- 所有界面和交互逻辑
- 路由切换、表单、消息展示、状态反馈
- 通过 `services/api.js` 访问本地 API

当前维护重点：

- `renderer/index.html` 必须保持为 Vite 入口页，并挂载 `/src/main.js`
- 聊天工作区默认首页为 `views/StudyWorkspaceView.vue`（单页应用，内部侧边导航切换模块）
- 聊天组件集中在 `components/workspace/`：ChatHistoryPanel、ChatPane、ComposerPanel、StatusPanel 等
- 页面级逻辑放在 `views/`，共用样式放在 `styles/`
- **注意**：项目不再使用 `layouts/` 目录（已删除），路由只有一条 `'/'→StudyWorkspaceView`

### 5.3 `1/server/`

职责：

- 本地 API 层
- SQLite 数据持久化 + FTS5 全文检索
- 文档解析（PDF/DOCX/MD/TXT）
- 文本切块与 RAG 检索
- 聊天、知识库、模型、智能体、工作流等业务逻辑

维护注意：

- 路由层只负责请求分发
- 业务逻辑统一进 `services/`
- RAG 相关服务三个文件：`documentParserService.js`、`chunkingService.js`、`ragService.js`
- 数据库初始化逻辑集中在 `config/database.js` 和 `db/schema.sql`
- `bootstrapDatabase()` 中包含自动迁移逻辑（如 `citations_json` 列）
- 新增接口时同步更新 API 文档

## 6. 环境变量维护

模板文件：[`1/.env.example`](/E:/基于Deepseek的智能体学习软件/1/.env.example)

当前变量：

- `PORT`：本地 API 服务端口（默认 3001）
- `VITE_API_BASE_URL`：前端访问后端的基础地址（默认 `http://127.0.0.1:3001/api`）
- `ELECTRON_RENDERER_URL`：开发模式下 Electron 加载的前端地址（默认 `http://127.0.0.1:5173`）

注意事项：

- 不要把真实密钥提交到仓库
- 建议使用 `.env.local` 保存私有配置

## 7. 数据维护

### 7.1 数据库位置

SQLite 数据库默认位于：

```text
1/server/data/app.db
```

同目录还会生成：

- `app.db-wal`
- `app.db-shm`

### 7.2 备份建议

版本升级或结构调整前建议备份：

- `1/server/data/app.db`
- `1/server/data/app.db-wal`
- `1/server/data/app.db-shm`

### 7.3 当前重点数据表

| 表名 | 用途 |
|------|------|
| `model_configs` | 模型配置 |
| `knowledge_bases` | 知识库 |
| `knowledge_documents` | 知识文档元数据 |
| `documents` | 文档记录 |
| `agents` | 智能体 |
| `learning_plans` | 学习计划 |
| `generated_resources` | 生成资源 |
| `questions` | 题目 |
| `answers` | 答案记录 |
| `mistakes` | 错题 |
| `chat_sessions` | 聊天会话 |
| `chat_messages` | 聊天消息（含 `citations_json`） |
| `chat_attachments` | 聊天附件 |
| `document_chunks` | RAG 文档切块 |
| `document_chunks_fts` | FTS5 全文索引（虚拟表） |

### 7.4 维护原则

- 避免直接手工改数据库内容
- 结构变更优先通过 `bootstrapDatabase()` 中的 `ALTER TABLE` 迁移
- 调整建表逻辑后同步更新 `schema.sql`
- 修改种子数据时注意不要破坏现有用户数据
- FTS5 虚拟表 `document_chunks_fts` 通过应用层手动同步（插入切块时同时写 FTS5，删除切块时同步清理）

## 8. 日常维护任务

### 8.1 模型配置维护

检查项：

- 默认模型是否存在
- Base URL 是否可达
- API Key 是否有效
- `is_default` 是否只保留一个默认值

### 8.2 聊天工作区维护

检查项：

- 默认首页是否正常进入聊天页
- `/api/chat/workspace` 是否能返回活动会话
- 会话标题是否正确更新
- 会话重命名和删除是否正常
- 附件登记后是否写入 `chat_attachments` 并解析索引
- 消息发送后是否写入 `chat_messages`（含 `citations_json`）
- 消息中是否包含 citations 引用数组

### 8.3 知识库维护

检查项：

- 知识库是否能正常创建
- 导入 PDF/DOCX 是否自动解析并切块
- `document_chunks` 和 `document_chunks_fts` 中是否有对应记录
- 删除文档时关联切块是否同步清理

### 8.4 RAG 检索维护

检查项：

- 上传 PDF 后 `document_chunks` 是否有切块记录
- 发送相关消息后 `assistantMessage.citations` 是否有返回
- 聊天页面是否显示"参考来源"卡片
- FTS5 搜索是否正常（`document_chunks_fts` 查询测试）

### 8.5 智能体维护

检查项：

- `retrieval` 和 `planning` 角色是否存在
- Prompt 是否与职责一致
- 模型绑定是否合理
- 是否存在角色重名或职责重叠

### 8.6 学习工作流维护

检查项：

- 诊断输出是否稳定
- 规划阶段是否完整
- 资源生成内容是否和输入匹配
- 反馈输出是否具备可执行性

## 9. 常见故障排查

### 9.1 前端无法启动

可能原因：

- 依赖未安装（特别是 `pdf-parse`、`mammoth` 等新依赖）
- Vite 端口被占用
- Node 版本过低

排查方式：

1. 检查 `npm install` 是否完成
2. 检查 `5173` 端口是否被占用
3. 执行 `node -v` 确认版本

### 9.2 Electron 打开空白页

可能原因：

- 前端开发服务器未启动
- `ELECTRON_RENDERER_URL` 配置错误
- 构建产物不存在

排查方式：

1. 确认开发模式下 `http://127.0.0.1:5173` 可访问
2. 确认生产模式下 `dist/renderer/index.html` 已生成
3. 检查 `renderer/index.html` 中存在 `#app` 和 `/src/main.js`

### 9.3 后端无法启动

可能原因：

- `3001` 端口冲突
- SQLite 文件权限异常
- FTS5 扩展不可用
- `pdf-parse` 或 `mammoth` 加载失败

排查方式：

1. 检查 `PORT` 是否被占用
2. 检查 `1/server/data/` 是否可写
3. 运行 `node server/src/index.js` 观察报错
4. 确认 `npm ls pdf-parse mammoth` 依赖已安装

### 9.4 RAG 检索无结果

可能原因：

- `document_chunks` 表为空（附件未上传或解析失败）
- `document_chunks_fts` 未同步
- 查询词与文档内容不匹配

排查方式：

1. 检查 `document_chunks` 是否有记录
2. 检查 `document_chunks_fts` 行数是否与 `document_chunks` 一致
3. 手动执行 SQL 测试 FTS5：`SELECT * FROM document_chunks_fts WHERE content MATCH '关键词'`
4. 检查服务端日志是否有解析错误

### 9.5 数据未写入

可能原因：

- 表结构未初始化（需重启服务触发 `bootstrapDatabase()`）
- 字段映射错误（如 `citations_json` 列不存在）
- 请求参数缺失或 schema 校验失败

排查方式：

1. 检查 `schema.sql` 是否包含新表
2. 检查 `bootstrapDatabase()` 中迁移逻辑是否执行
3. 检查服务层字段命名是否一致
4. 检查前端请求体是否带上必要字段

## 10. 代码维护规范

建议遵循：

- 页面只处理展示和交互
- 业务逻辑集中在 `server/src/services/`
- API 封装统一放在 `renderer/src/services/`
- 每次新增模块同步更新四份说明文档
- 数据结构变更后同步更新建表脚本和 API 文档

命名建议：

- 页面组件：`XxxView.vue`
- 服务文件：`xxxService.js`
- 路由文件：`xxx.js`
- 数据表名：snake_case 复数英文

## 11. 版本升级建议

建议按以下顺序演进：

1. ~~接入真实 OpenAI-Compatible 对话接口~~（已实现）
2. ~~增加真实附件落盘与文档解析~~（已实现 PDF/DOCX/MD/TXT）
3. 引入向量检索（sqlite-vec）替代 FTS5，提升语义召回精度
4. 为更多智能体建立独立执行链路
5. 增加导出、日志、自动更新和打包

## 12. 当前维护结论

当前项目已具备：

- 可运行的 Electron + Vue + Express + SQLite 架构
- 默认聊天首页和本地 API 联调
- 会话、消息、附件持久化
- 会话重命名、删除、清空
- PDF / DOCX 文档解析 + 自动切块 + FTS5 全文索引
- RAG 检索召回 + LLM 上下文注入 + 引用展示
- 外部模型接入（DeepSeek / OpenAI-Compatible）
- 模型、知识库、智能体、学习工作流基础能力

当前项目仍需继续完善：

- 向量语义检索
- 流式响应（SSE）
- 更多文档格式支持（PPT、XLSX）
- 打包发布与自动更新
