# 前端设计落地实现步骤文档

## 0. 总体原则
- 保持现有技术栈：`Vue 3 + Pinia + Vue Router + Element Plus + Electron + Express + SQLite`。
- 将“前端设计文档”视为**功能规格**，不做整栈迁移到 React。
- 采用分阶段交付：每个阶段都有可运行结果、可验证接口、可回滚边界。

---

## 1. 阶段一：路由与布局骨架落地
### 目标
先把信息架构搭起来，让页面结构与文档主路由一致。

### 实现步骤
1. 扩展前端路由：在 `1/renderer/src/router/index.js` 增加 `dashboard` 主路由及子路由骨架。
2. 新建布局组件：
   - `PublicLayout.vue`
   - `DashboardLayout.vue`
3. 将现有 `StudyWorkspaceView.vue` 迁移为 `/dashboard/chat` 页面。
4. 为关键模块先建占位页（空壳页面）：
   - `DashboardHome`
   - `StudySets`
   - `KnowledgeBase`
   - `Analytics`
   - `Settings`
5. 调整导航与菜单，保证页面可切换。

### 产出物
- 路由树可访问。
- 左侧导航与顶部区域可用。
- 现有聊天能力不受影响。

### 验收标准
- 启动后可在应用内切换到所有一级路由。
- 路由刷新不报错，布局稳定。

---

## 2. 阶段二：学习集（Study Sets）基础能力
### 目标
完成学习集 CRUD 与详情页主结构，为后续闪卡、测验、笔记打底。

### 实现步骤
1. 数据库新增表：
   - `study_sets`
   - `study_set_tags`
2. 后端新增模块：
   - `routes/studySets.js`
   - `services/studySetService.js`
3. 接口实现：
   - `GET /api/study-sets`
   - `POST /api/study-sets`
   - `GET /api/study-sets/:id`
   - `PUT /api/study-sets/:id`
   - `DELETE /api/study-sets/:id`
4. 前端新增：
   - `stores/studySets.js`
   - `views/StudySetsPage.vue`
   - `views/StudySetDetailPage.vue`
5. 实现列表筛选、创建、编辑、删除、详情展示。

### 产出物
- 学习集完整基础功能可用。

### 验收标准
- 学习集增删改查全链路打通。
- 刷新后数据持久化正确。

---

## 3. 阶段三：闪卡与间隔重复学习
### 目标
实现核心学习闭环：闪卡创建、复习、SM-2 调度。

### 实现步骤
1. 数据库新增表：
   - `flashcards`
   - `flashcard_reviews`
2. 后端新增接口：
   - `GET /api/study-sets/:id/flashcards`
   - `POST /api/study-sets/:id/flashcards`
   - `PUT /api/flashcards/:id`
   - `DELETE /api/flashcards/:id`
   - `POST /api/flashcards/:id/review`
   - `GET /api/flashcards/due`
3. 实现 `SM-2` 算法服务层封装。
4. 前端新增：
   - `stores/flashcards.js`
   - `FlashcardEditor`、`ClozeEditor`（先做标准卡 + 完形填空）
   - `StudySessionPage.vue`
5. 完成复习流程：翻卡、评分、更新下次复习时间。

### 产出物
- 到期复习卡片可自动调度。

### 验收标准
- 复习评分后卡片状态变化正确。
- 下次复习时间符合 SM-2 规则。

---

## 4. 阶段四：测验（Quiz）模块
### 目标
支持学习集生成测验、作答、评分、历史记录。

### 实现步骤
1. 数据库新增：
   - `quizzes`
   - `quiz_questions`
   - `quiz_attempts`
2. 后端实现：
   - `POST /api/quizzes/generate`
   - `GET /api/quizzes/study-set/:id`
   - `POST /api/quizzes/:id/attempts`
   - `GET /api/quizzes/:id/attempts`
3. 前端实现：
   - `stores/quizzes.js`
   - `QuizPage.vue`
   - `QuizResultPanel.vue`
4. 先支持题型：单选、判断、填空（简答后续补）。

### 产出物
- 题目生成到提交结果全链路可用。

### 验收标准
- 能创建测验并完成一次作答。
- 作答后可查看分数和解析。

---

## 5. 阶段五：笔记、文档、知识来源联动
### 目标
完成学习集详情页多 Tab 能力（笔记/文档/来源）。

### 实现步骤
1. 复用并扩展现有知识库文档能力。
2. 新增笔记数据结构与接口：
   - `notes` 表 + CRUD API
3. 详情页增加 Tab：
   - 闪卡
   - 测验
   - 学习计划
   - 笔记
   - 文档
   - 来源
4. 完成笔记置顶、颜色标记、来源类型。

### 产出物
- 学习集详情具备完整信息组织能力。

### 验收标准
- 各 Tab 独立加载与操作正常。
- 文档与笔记可与学习集正确关联。

---

## 6. 阶段六：实时与流式能力统一
### 目标
统一 Chat / Problem Solver / Research 的流式协议，并引入实时竞赛基础设施。

### 实现步骤
1. 统一 SSE 事件格式（start/delta/done/error）。
2. 抽象前端流式消费工具（`services/streamClient.js`）。
3. 引入 WebSocket（Socket.IO）基础层。
4. 搭建 Live Quiz 最小可用版本：
   - 创建房间
   - 加入房间
   - 推题与积分板

### 产出物
- SSE 与 WS 并存架构稳定。

### 验收标准
- 聊天和求解流式输出不中断。
- 两个客户端可在同一房间实时互动。

---

## 7. 阶段七：Problem Solver / Teach-Back / Learning Paths
### 目标
按优先级落地高级学习功能。

### 实现步骤
1. Problem Solver（优先）：
   - 输入题目
   - 流式显示分析/求解/验证阶段
   - 保存会话与历史
2. Teach-Back：
   - 创建讲解会话
   - 提交解释
   - AI 评分与反馈
3. Learning Paths：
   - 生成路径
   - 步骤完成与进度追踪

### 产出物
- 三大高级学习模块可用。

### 验收标准
- 每个模块至少完成“创建 -> 执行 -> 结果查看”闭环。

---

## 8. 阶段八：体验完善与质量收敛
### 目标
提升可用性、稳定性、可维护性，达到发布标准。

### 实现步骤
1. 主题系统统一（亮/暗/系统）并修复当前主题逻辑硬编码问题。
2. 增加 i18n 架构（先中英双语）。
3. 加强错误处理：
   - 页面级 Error Boundary（Vue 对应方案）
   - API 统一错误提示
4. 补测试：
   - 后端路由集成测试
   - 前端关键 store 与流程测试
5. 完成文档：
   - API 更新
   - 迁移说明
   - 运维与启动说明

### 产出物
- 可稳定运行的完整版本。

### 验收标准
- 核心流程无阻断问题。
- 关键接口与页面有测试覆盖。

---

## 9. 推荐执行节奏（里程碑）
1. M1（阶段1-2）：路由骨架 + 学习集 CRUD。
2. M2（阶段3-4）：闪卡复习 + 测验闭环。
3. M3（阶段5-6）：详情整合 + 实时能力基建。
4. M4（阶段7-8）：高级模块 + 质量收敛发布。

---

## 10. 风险与决策点
1. 若坚持 React 设计稿逐像素迁移：成本高、需重构前端栈，不建议。
2. 文档覆盖功能极广：应按里程碑分批上线，避免一次性大改。
3. 实时模块（WebSocket）与流式模块（SSE）并存：需统一事件规范，避免前端复杂度失控。

---

## 11. 下一步立即执行项
1. 先进入阶段一，完成路由与 `DashboardLayout` 骨架。
2. 保持现有聊天功能稳定，作为后续模块集成基座。
3. 同步创建 `study_sets` 数据表与最小 CRUD 接口，为阶段二铺路。
