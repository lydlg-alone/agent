# 详细技术说明文档

本文档说明当前版本的整体架构、模块职责、数据流以及最近这轮附件与 OCR 能力的实现方式。

## 1. 总体架构

项目是一个本地桌面三层结构：

```text
Electron
  └─ Renderer (Vue 3 + Vite)
      └─ Local API Server (Express)
          └─ SQLite
```

职责划分：

- `Electron`
  - 提供桌面窗口、preload 和本地壳层
- `Renderer`
  - 提供聊天、知识库、统计、模型配置等界面
- `Express`
  - 提供业务 API
- `SQLite`
  - 存储会话、消息、知识库、配置与检索数据

## 2. 前端结构

核心页面：

- `renderer/src/views/StudyWorkspaceView.vue`
  - 主工作区容器
  - 串联聊天区、状态区、历史会话和输入区
- `renderer/src/views/AnalyticsView.vue`
  - 统计页面

核心组件：

- `ChatPane.vue`
  - 消息列表
  - 助手回复渲染
  - 聊天气泡中的附件卡片
- `ComposerPanel.vue`
  - 文本输入框
  - 上传按钮
  - 输入区附件卡片
  - `联网` / `结构化` 两个可见切换按钮
- `AttachmentCard.vue`
  - 统一的文件卡片 UI
  - 支持输入区和消息区两种展示样式
- `StatusPanel.vue`
  - 当前模型、知识库、智能体状态

前端工具状态特点：

- `useTools` 固定为默认开启
- `useHybridRetrieval` 固定为默认开启
- `useImageVision` 固定为默认开启
- 用户当前只切换：
  - `useWebSearch`
  - `useStructuredOutput`

## 3. 后端结构

入口文件：

- `server/src/app.js`
- `server/src/routes/index.js`

路由分组：

- `routes/chat.js`
- `routes/knowledge.js`
- `routes/models.js`
- `routes/agents.js`
- `routes/workflows.js`
- `routes/dashboard.js`
- `routes/analytics.js`
- `routes/health.js`

后端启动时会：

1. 初始化数据库
2. 执行 schema
3. 尝试补齐历史缺失字段
4. 注册全部路由

## 4. 聊天主流程

主逻辑文件：

```text
1/server/src/services/chatWorkspaceService.js
```

普通消息流程：

1. 接收 `content`、`attachmentIds` 和工具选项
2. 读取当前会话、历史消息和附件
3. 根据配置生成系统提示与上下文
4. 执行知识库检索
5. 按需执行网页搜索
6. 构造模型请求消息
7. 按需处理工具调用闭环
8. 保存用户消息与助手消息

流式消息流程：

1. 入参校验与普通消息一致
2. 后端通过 SSE 推送 `start`
3. 持续推送 `delta`
4. 完成后推送 `done`
5. 出错时推送 `error`

## 5. 附件流程

### 5.1 上传

入口：

- `POST /api/chat/attachments`

流程：

1. 接收附件元信息和内容
2. 写入 `chat_attachments`
3. 若是文本附件，抽取正文摘要并切片索引
4. 若是图片附件，保留 `content_data`
5. 图片附件再尝试 OCR，生成 `content_excerpt`

### 5.2 待发送附件删除

入口：

- `DELETE /api/chat/attachments/:id`

流程：

1. 删除附件记录
2. 清除附件在检索系统中的索引块
3. 返回当前会话剩余附件列表

### 5.3 发送时关联

聊天发送时，前端把当前输入框中的 `pendingAttachments` 映射成 `attachmentIds` 提交给后端。后端会：

- 把附件摘要放入系统提示上下文
- 把附件名称放入用户消息上下文
- 对图片附件决定是否走视觉输入链路

## 6. 图片 OCR 与视觉输入

### 6.1 OCR 服务

文件：

```text
1/server/src/services/imageOcrService.js
```

实现要点：

- 使用 `tesseract.js`
- 默认语言：`chi_sim` + `eng`
- OCR worker 懒加载并复用
- Core 资源从本地 `node_modules/tesseract.js-core` 读取
- 识别失败时终止 worker 并静默降级

OCR 关键路径：

- `OCR_CACHE_PATH`
  - 默认在 `data/ocr-cache`
- `OCR_LANG_PATH`
  - 如设置则优先从环境变量指定位置读取语言数据

### 6.2 视觉输入消息

如果模型支持视觉输入，图片消息会被组装成：

```json
{
  "type": "image_url",
  "image_url": {
    "url": "data:image/png;base64,..."
  }
}
```

### 6.3 模型能力判断

关键函数：

- `supportsNativeVision(runtime, modelId)`

当前逻辑：

- DeepSeek 明确视为不支持原生视觉输入
- OpenAI、Qwen、Ollama 按 provider、baseUrl 和 modelId 关键词识别
- 其他供应商按模型名兜底判断

### 6.4 兼容性降级

为避免上游接口报：

```text
unknown variant 'image_url'
```

当前实现做了两层保护：

1. 请求发出前，如果模型判断为不支持视觉，先把多模态消息转成纯文本消息
2. 如果上游仍因 `image_url` 报错，再剥离视觉内容并自动重试一次

这意味着：

- 非视觉模型不会因为图片附件导致会话直接失败
- 图片中的文字优先由 OCR 进入上下文
- 即使 OCR 失败，也不会影响普通问答链路

## 7. 工具调用与结构化输出

工具定义位于：

- `buildToolDefinitions()`

当前内置工具：

- `search_knowledge`
- `web_search`

工具调用闭环：

1. 第一轮模型请求允许返回 `tool_calls`
2. 后端解析工具调用
3. 本地执行工具函数
4. 将工具结果作为 `role: "tool"` 回填
5. 发起第二轮模型请求生成最终回复

结构化输出：

- 当 `useStructuredOutput=true` 时，后端会附加 `response_format: { type: "json_object" }`
- 同时在系统提示中加入结构化输出约束

## 8. 知识库与混合检索

关键文件：

- `knowledgeBaseService.js`
- `documentParserService.js`
- `chunkingService.js`
- `ragService.js`

检索模式：

- `keyword`
  - SQLite FTS5
- `vector`
  - 本地轻量向量相似度
- `hybrid`
  - 合并关键词召回与向量召回，再 rerank

当前向量方案：

- 本地轻量哈希向量
- 不依赖外部 embedding 服务
- 成本低，但语义能力弱于真实 embedding 模型

## 9. 启动机制

文件：

- `scripts/start-electron.cjs`
- `package.json`

当前启动方式：

- `npm run dev`
  - 启动前端
  - 启动后端
  - 等待端口就绪
  - 启动 Electron

关键修复：

- 启动 Electron 前删除 `ELECTRON_RUN_AS_NODE`
- 避免 Electron 被误当成 Node 进程运行

## 10. 数据库设计

核心表：

- `chat_sessions`
- `chat_messages`
- `chat_attachments`
- `knowledge_bases`
- `knowledge_documents`
- `document_chunks`
- `document_chunks_fts`
- `agents`
- `connection_history`

与最近能力相关的关键字段：

- `chat_attachments.content_data`
  - 保存图片 data URL
- `chat_attachments.content_excerpt`
  - 保存附件摘要与 OCR 文本
- `document_chunks.embedding_json`
  - 保存本地向量

## 11. 配置存储

运行时配置相关文件：

- `server/src/services/runtimeConfigService.js`
- `server/src/services/apiConfigFileService.js`

默认位置：

```text
%USERPROFILE%\Documents\agent API\runtime-config.json
```

可用环境变量：

- `AGENT_CONFIG_DIR`
- `AGENT_DATA_DIR`
- `TESSERACT_LANG_PATH`

## 12. 当前测试覆盖

集成测试文件：

```text
1/server/test/app.integration.test.js
```

覆盖点：

- mock 聊天回复
- 流式聊天 SSE
- 知识库导入与检索
- 图片附件上传
- 带图片附件继续聊天
- 待发送附件删除
- 运行时配置保存与读取
- 空消息校验

## 13. 当前限制

- OCR 准确率取决于图片质量和语言数据可用性
- 首次 OCR 可能依赖语言数据下载或本地语言包
- 轻量向量检索不是高质量 embedding 检索
- 结构化输出当前仍是通用 JSON 约束，不是业务级 schema
- 多智能体目前更接近“角色化协同”，还不是完整编排系统

## 14. 后续建议

1. 为 OCR 提供离线语言包部署方案
2. 为不同模型维护更精确的视觉能力表
3. 把轻量向量升级为真实 embedding 服务
4. 为不同工作流补充严格 schema
5. 为附件卡片补充图片预览与拖拽上传
