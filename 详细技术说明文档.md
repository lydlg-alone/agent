# 详细技术说明文档

本文档说明当前项目的整体架构、核心模块、数据流和已实现能力。

## 1. 总体架构

项目采用桌面端单仓结构：

- `Electron` 负责桌面窗口与本地宿主环境
- `Vue 3` 负责前端界面与交互
- `Express` 提供本地 HTTP API
- `SQLite` 保存本地业务数据

运行关系如下：

```text
Electron
  ├─ Renderer(Vite + Vue 3)
  └─ Local API Server(Express)
           └─ SQLite
```

## 2. 前端结构

前端主页面位于：

```text
1/renderer/src/views/StudyWorkspaceView.vue
```

当前已将工作区拆分为六块：

- `ChatPane.vue`：聊天消息区
- `ComposerPanel.vue`：底部输入区
- `StatusPanel.vue`：右侧状态栏
- `SettingsForm.vue`：设置表单
- `KnowledgeList.vue`：知识库列表
- `useRuntimeSubscription.js`：运行时配置订阅

状态管理：

- `runtime.js`：运行时配置、模型状态、配置路径、连接状态

前端接口层：

- `renderer/src/services/api.js`

已支持能力：

- 聊天流式输出
- 历史会话加载与切换
- 新会话自动保存
- 会话重命名、删除、清空
- 运行时配置自动读取
- 配置外部修改后的实时同步

## 3. 后端结构

后端入口：

```text
1/server/src/index.js
1/server/src/app.js
```

路由分层：

- `routes/chat.js`
- `routes/models.js`
- `routes/knowledge.js`
- `routes/agents.js`
- `routes/dashboard.js`
- `routes/workflows.js`

服务层：

- `chatWorkspaceService.js`：聊天工作区主服务
- `runtimeConfigService.js`：运行时配置读写与订阅
- `modelConfigService.js`：模型配置与连接测试
- `knowledgeBaseService.js`：知识库导入、删除、检索测试
- `agentService.js`：智能体列表与激活
- `ragService.js`：文档块索引、全文召回、引用提示构建
- `documentParserService.js`：解析 PDF、DOCX、文本
- `chunkingService.js`：文本切块
- `legacyDataCleanupService.js`：历史乱码清洗

## 4. 运行时配置设计

当前已收敛为单一配置源：

```text
文档\agent API\runtime-config.json
```

设计目标：

- 不在项目目录内部保存用户 API 配置
- 跟随系统真实“文档”目录，不写死路径
- 支持前端自动读取和实时刷新

安全策略：

- Windows 下 `API Key` 不再明文保存
- 读取配置时自动解密并提供给后端服务调用
- 导出配置时不会返回密钥明文

## 5. 聊天链路

### 5.1 会话生命周期

聊天会话保存在：

- `chat_sessions`
- `chat_messages`
- `chat_attachments`

当前支持：

- 创建会话
- 查询会话详情
- 重命名会话
- 删除会话
- 清空会话消息

### 5.2 发送消息

普通消息接口：

```text
POST /api/chat/messages
```

流式消息接口：

```text
POST /api/chat/messages/stream
```

处理流程：

1. 前端提交 `sessionId`、消息内容和附件 ID
2. 后端读取当前激活智能体与当前运行时模型
3. 如果有知识文档，执行检索召回
4. 组装系统提示词、知识片段和用户输入
5. 调用外部模型接口或本地 mock 回复
6. 将用户消息和助手消息写入 SQLite
7. 返回完整回复或按 `SSE` 分片输出

### 5.3 流式输出

流式接口返回 `text/event-stream`，事件包含：

- `start`
- `delta`
- `done`
- `error`

前端策略：

- 先插入用户消息
- 再插入一个流式助手占位消息
- 随 `delta` 持续追加文本
- `done` 后刷新会话详情，保证前后端状态一致

## 6. 知识库与检索

知识库相关表：

- `knowledge_bases`
- `knowledge_documents`
- `document_chunks`
- `document_chunks_fts`

导入流程：

1. 前端提交文件名、类型、原始文本或解析后内容
2. 后端按类型解析文档
3. 生成摘要与块信息
4. 将块写入 `document_chunks`
5. 同步写入 `document_chunks_fts`

当前检索实现：

- 基于 SQLite `FTS5`
- 以关键词召回为主
- 回答中可返回引用信息

当前不是向量数据库方案，属于轻量本地检索增强实现。

## 7. 智能体设计

智能体表：

- `agents`

当前支持：

- 智能体列表读取
- 激活指定智能体
- 工作区显示当前激活智能体

智能体的主要作用：

- 为聊天构建不同角色语气和任务边界
- 与系统提示词、知识库内容共同参与最终回答

## 8. 数据库结构

当前核心表包括：

- `model_configs`
- `knowledge_bases`
- `knowledge_documents`
- `document_chunks`
- `agents`
- `chat_sessions`
- `chat_messages`
- `chat_attachments`
- `connection_history`

兼容表仍保留：

- `documents`
- `learning_plans`
- `generated_resources`
- `questions`
- `answers`
- `mistakes`

说明：

- 当前知识库主链路已转向 `knowledge_documents + document_chunks`
- `FTS5` 虚拟表 `document_chunks_fts` 用于快速文本检索

## 9. 校验与稳定性

后端已加统一 schema 校验层，主要放在：

```text
1/server/src/utils/schemaValidation.js
```

作用：

- 校验路由参数
- 校验查询参数
- 校验请求体
- 尽量在进入 service 之前拦截非法输入

已覆盖的重点路由：

- 聊天
- 模型配置
- 知识库
- 智能体

## 10. 测试现状

最小集成测试文件：

```text
1/server/test/app.integration.test.js
```

当前覆盖：

- 普通聊天
- 流式聊天
- 知识库导入
- 配置保存和读取
- schema 校验错误返回

测试目标：

- 保证核心工作区链路在改动后可快速回归

## 11. 已知限制

- 当前检索仍是本地全文召回，不是向量召回
- 工作流接口已存在，但智能体协作仍以单主工作区为核心
- 当前没有用户鉴权和多账号隔离
- 仍以本地开发和单机使用为主要目标
- Electron 打包、自动更新和安装包链路尚未完整收口

## 12. 后续建议

推荐后续按以下顺序演进：

1. 完整收口聊天页剩余细节和历史记录体验
2. 继续增强知识库召回质量和引用展示
3. 为学习工作流增加真实多步骤执行链路
4. 补更完整的前后端自动化测试
5. 增加打包、升级和日志导出能力
