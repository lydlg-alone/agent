# 详细技术说明文档

本文档说明当前项目的整体架构、核心模块、数据流和已实现能力。

## 1. 总体架构

项目采用桌面端单仓结构：

- `Electron` 负责桌面窗口与本地宿主环境
- `Vue 3` 负责前端界面与交互
- `Express` 提供本地 HTTP API
- `SQLite` 保存本地业务数据

运行关系如下：

```text
Electron
  ├─ Renderer(Vite + Vue 3)
  └─ Local API Server(Express)
           └─ SQLite
```

## 2. 前端结构

前端主页面位于：

```text
1/renderer/src/views/StudyWorkspaceView.vue
```

当前已将工作区拆分为以下组件：

- `ChatPane.vue`：聊天消息区（含 Markdown/LaTeX 渲染）
- `ChatHistoryPanel.vue`：历史会话列表
- `ComposerPanel.vue`：底部输入区
- `StatusPanel.vue`：右侧状态栏
- `SettingsForm.vue`：设置表单
- `KnowledgeList.vue`：知识库列表
- `AnalyticsView.vue`：学习仪表盘（ECharts 图表）
- `useRuntimeSubscription.js`：运行时配置 SSE 订阅

状态管理（Pinia stores）：

- `runtime.js`：运行时配置、模型状态、配置路径、连接状态
- `theme.js`：主题模式（light/dark/system）、localStorage 持久化、系统偏好监听

前端接口层：

- `renderer/src/services/api.js`

已支持能力：

- 聊天流式输出
- Markdown 渲染（marked + highlight.js + KaTeX）
- 暗色模式（CSS 变量体系 + data-theme 切换）
- 历史会话加载与切换
- 新会话自动保存
- 会话重命名、删除、清空
- 运行时配置自动读取
- 配置外部修改后的实时同步
- 学习仪表盘数据可视化

## 3. Markdown 渲染管线

渲染管线位于：

```text
1/renderer/src/utils/markdownRenderer.js
```

处理流程：

```text
原始文本
  → KaTeX 预处理（$...$ / $$...$$ 提取为占位符）
  → marked 解析 GFM（表格、任务列表、删除线、链接等）
  → highlight.js 代码语法高亮
  → KaTeX 占位符替换回渲染 HTML
  → DOMPurify 安全过滤（XSS 防护，白名单允许 KaTeX SVG/MathML 标签）
  → 安全 HTML 输出
```

关键设计：

- KaTeX 采用**预处理占位符方案**：在 marked 解析前将公式替换为唯一 ID，解析完成后再替换回，避免 marked 误将 `$`、`_`、`^` 等字符解析为 Markdown 语法
- DOMPurify 配置了扩展白名单（`svg`、`path`、`math`、`mrow` 等 KaTeX 输出标签）
- 通过 `data-theme` 属性自适应明暗主题的代码/公式颜色

依赖版本建议：

- `marked` ^15.0
- `highlight.js` ^11.10
- `katex` ^0.16
- `dompurify` ^3.2

## 4. 暗色模式设计

主题系统采用 **CSS 变量 + data-theme 属性** 方案：

```
:root                    → 亮色变量默认值
[data-theme="dark"]      → 暗色变量覆盖值
```

核心文件：

```text
1/renderer/src/styles/theme.css    CSS 变量体系（50+ 变量）
1/renderer/src/stores/theme.js     Pinia store（三态切换逻辑）
```

设计目标：

- 所有组件颜色通过 `var(--xxx)` 引用，不写死色值
- 三态切换：`light` / `dark` / `system`（跟随 OS）
- 状态持久化在 `localStorage`（键 `ai-study-theme-mode`）
- 监听 `prefers-color-scheme` 媒体查询自动跟随系统
- Element Plus 暗色支持通过官方 `element-plus/theme-chalk/dark/css-vars.css` 实现
- highlight.js / KaTeX 暗色覆盖在 `theme.css` 中定义

## 5. 后端结构

后端入口：

```text
1/server/src/index.js
1/server/src/app.js
```

路由分层：

- `routes/chat.js`：聊天工作区
- `routes/models.js`：模型配置与测试
- `routes/knowledge.js`：知识库管理
- `routes/agents.js`：智能体管理
- `routes/dashboard.js`：首页摘要
- `routes/analytics.js`：学习数据统计
- `routes/workflows.js`：学习工作流

服务层：

- `chatWorkspaceService.js`：聊天工作区主服务（消息发送、流式输出、RAG 召回）
- `runtimeConfigService.js`：运行时配置读写与订阅
- `modelConfigService.js`：模型配置与连接测试
- `knowledgeBaseService.js`：知识库导入、删除、检索测试
- `agentService.js`：智能体列表与激活
- `analyticsService.js`：学习数据聚合统计（新增）
- `learningWorkflowService.js`：学习工作流（诊断/计划/资源/练习/反馈）
- `ragService.js`：文档块索引、全文召回、引用提示构建
- `documentParserService.js`：解析 PDF、DOCX、文本
- `chunkingService.js`：文本切块
- `legacyDataCleanupService.js`：历史乱码清洗

## 6. 运行时配置设计

当前已收敛为单一配置源：

```text
文档\agent API\runtime-config.json
```

设计目标：

- 不在项目目录内部保存用户 API 配置
- 跟随系统真实"文档"目录，不写死路径
- 支持前端自动读取和实时刷新

安全策略：

- Windows 下 `API Key` 不再明文保存
- 读取配置时自动解密并提供给后端服务调用
- 导出配置时不会返回密钥明文

## 7. 聊天链路

### 7.1 会话生命周期

聊天会话保存在：

- `chat_sessions`
- `chat_messages`
- `chat_attachments`

当前支持：

- 创建会话
- 查询会话详情
- 重命名会话
- 删除会话
- 清空会话消息

### 7.2 发送消息

普通消息接口：

```text
POST /api/chat/messages
```

流式消息接口：

```text
POST /api/chat/messages/stream
```

处理流程：

1. 前端提交 `sessionId`、消息内容和附件 ID
2. 后端读取当前激活智能体与当前运行时模型
3. 如果有知识文档，执行检索召回（FTS5）
4. 组装系统提示词、知识片段和用户输入
5. 调用外部模型接口或本地 mock 回复
6. 将用户消息和助手消息写入 SQLite
7. 返回完整回复或按 `SSE` 分片输出

### 7.3 流式输出

流式接口返回 `text/event-stream`，事件包含：

- `start`
- `delta`
- `done`
- `error`

前端策略：

- 先插入用户消息
- 再插入一个流式助手占位消息
- 随 `delta` 持续追加文本
- `done` 后刷新会话详情，保证前后端状态一致

### 7.4 消息渲染

前端收到消息后通过 `markdownRenderer.js` 渲染为 HTML：

- GFM 标准 Markdown → `marked`
- 代码块 → `highlight.js`（自动检测语言或按标注语言高亮）
- LaTeX 公式 → `katex`（`$...$` 行内 / `$$...$$` 块级）
- 安全过滤 → `DOMPurify`

## 8. 知识库与检索

知识库相关表：

- `knowledge_bases`
- `knowledge_documents`
- `document_chunks`
- `document_chunks_fts`

导入流程：

1. 前端提交文件名、类型、原始文本或解析后内容
2. 后端按类型解析文档（`documentParserService.js`）
3. 生成摘要与块信息（`chunkingService.js`）
4. 将块写入 `document_chunks`
5. 同步写入 `document_chunks_fts`

当前检索实现：

- 基于 SQLite `FTS5`
- 以关键词召回为主
- 回答中可返回引用信息

当前不是向量数据库方案，属于轻量本地检索增强实现。

## 9. 学习仪表盘

仪表盘后端服务位于：

```text
1/server/src/services/analyticsService.js
```

提供接口：

```text
GET /api/analytics/overview
```

聚合统计内容：

- 基础计数：会话数、消息数（按 user/assistant 分）、文档数/总大小、知识库数、智能体数、练习题数、答案数、生成资源数、计划数、书签数
- 每日活跃度：近 7 天每天消息数（user/assistant 分列）
- 练习趋势：近 30 天每日平均得分
- 文档分布：按 `source_type` 分组的文档数量
- 最近记录：最近 5 份文档、最近 5 个会话

前端 `AnalyticsView.vue` 使用 **ECharts 按需导入**（仅 LineChart + PieChart + 必要组件），包含：

- 每日对话活跃度折线图（用户消息 / AI 回复双线）
- 练习得分趋势面积图
- 文档类型分布饼图
- 6 个统计概览卡片
- 暗色模式下图表颜色自适应

## 10. 智能体设计

智能体表：

- `agents`

当前支持：

- 智能体列表读取
- 激活指定智能体
- 工作区显示当前激活智能体

智能体的主要作用：

- 为聊天构建不同角色语气和任务边界
- 与系统提示词、知识库内容共同参与最终回答

## 11. 学习工作流

工作流接口：

- `POST /api/workflows/diagnose`：诊断学习状态
- `POST /api/workflows/plan`：生成学习计划
- `POST /api/workflows/resources`：生成学习资料
- `POST /api/workflows/practice`：生成练习内容
- `POST /api/workflows/feedback`：评估答案并返回反馈

每个接口均支持两种模式：

- **LLM 模式**：当运行时配置有效时，调用外部 AI 模型生成内容
- **Mock 降级**：当 LLM 不可用时，回退到本地模板生成

生成的资源会持久化到对应 SQLite 表（`learning_plans`、`generated_resources`、`questions`、`answers`、`mistakes`）。

## 12. 数据库结构

当前核心表包括：

- `model_configs`
- `knowledge_bases`
- `knowledge_documents`
- `document_chunks`
- `agents`
- `chat_sessions`
- `chat_messages`
- `chat_attachments`
- `connection_history`

兼容表仍保留：

- `documents`
- `learning_plans`
- `generated_resources`
- `questions`
- `answers`
- `mistakes`

说明：

- 当前知识库主链路已转向 `knowledge_documents + document_chunks`
- `FTS5` 虚拟表 `document_chunks_fts` 用于快速文本检索
- `chat_messages` 支持 `bookmarked` 字段（收藏标记）
- 仪表盘统计直接查询上述各表聚合

## 13. 校验与稳定性

后端已加统一 schema 校验层，主要放在：

```text
1/server/src/utils/schemaValidation.js
```

作用：

- 校验路由参数
- 校验查询参数
- 校验请求体
- 尽量在进入 service 之前拦截非法输入

已覆盖的重点路由：

- 聊天
- 模型配置
- 知识库
- 智能体
- 仪表盘统计

## 14. 测试现状

最小集成测试文件：

```text
1/server/test/app.integration.test.js
```

当前覆盖：

- 普通聊天
- 流式聊天
- 知识库导入
- 配置保存和读取
- schema 校验错误返回

测试目标：

- 保证核心工作区链路在改动后可快速回归

## 15. 已知限制

- 当前检索仍是本地全文召回（FTS5），不是向量召回
- 工作流接口已存在，LLM 接入需完成 prompt 工程调优
- 当前没有用户鉴权和多账号隔离
- 仍以本地开发和单机使用为主要目标
- Electron 打包、自动更新和安装包链路尚未完整收口
- URL 网页导入功能尚未实现

## 16. 后续建议

推荐后续按以下顺序演进：

1. URL 网页导入知识库
2. 消息收藏/书签功能
3. 间隔重复/闪卡系统
4. 继续增强知识库召回质量（考虑向量化方案）
5. Ollama 本地模型支持
6. 补更完整的前后端自动化测试
7. 增加打包、升级和日志导出能力
