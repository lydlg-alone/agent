# 详细技术说明文档

本文档描述当前版本的整体架构、模块职责、数据流和本次新增能力的实现方式。

## 1. 总体架构

项目是一个典型的本地桌面三层结构：

```text
Electron
  ├─ Renderer（Vue 3 + Vite）
  └─ Local API Server（Express）
       └─ SQLite
```

职责划分：

- `Electron`
  - 提供桌面窗口与 preload
- `Renderer`
  - 提供聊天界面、知识库、统计页、设置页
- `Express`
  - 提供聊天、模型、知识库、智能体、工作流、统计等 API
- `SQLite`
  - 保存会话、消息、文档、切片、练习、配置和连接历史

## 2. 前端结构

核心页面：

- `renderer/src/views/StudyWorkspaceView.vue`
  - 主工作区容器
  - 串联聊天区、知识库区、状态区和能力开关状态
- `renderer/src/views/AnalyticsView.vue`
  - 统计页

核心组件：

- `ChatPane.vue`
  - 消息列表、快捷提示词、引用展示
- `ComposerPanel.vue`
  - 输入框、附件按钮、五个能力开关、发送按钮、状态提示
- `KnowledgeList.vue`
  - 文档列表、搜索、文件导入、URL 导入
- `StatusPanel.vue`
  - 当前模型、知识库、智能体状态

前端 API 封装：

- `renderer/src/services/api.js`

说明：

- 聊天消息既支持非流式，也支持 SSE 流式
- 当前 URL 导入接口和五个能力开关都已经接入前端调用

## 3. 后端结构

入口：

- `server/src/app.js`
- `server/src/routes/index.js`

后端在启动时会：

1. 初始化数据库
2. 执行初始数据种子
3. 执行历史脏数据修复
4. 注册全部 API 路由

路由分组：

- `routes/chat.js`
- `routes/knowledge.js`
- `routes/models.js`
- `routes/agents.js`
- `routes/workflows.js`
- `routes/dashboard.js`
- `routes/analytics.js`
- `routes/health.js`

## 4. 聊天流程

聊天主逻辑位于：

```text
1/server/src/services/chatWorkspaceService.js
```

### 4.1 普通发送流程

1. 接收用户消息、附件 ID 和五个能力开关
2. 补全会话上下文和当前模型配置
3. 根据开关决定是否执行：
   - 本地知识检索
   - 网页搜索
   - 工具调用
   - 结构化输出约束
   - 图片视觉输入
4. 调用模型
5. 保存用户消息与助手消息
6. 返回最终结果

### 4.2 流式发送流程

- 普通模式下通过 SSE 逐段返回 `delta`
- 当开启 `工具` 或 `结构化` 时，后端会先整合结果，再输出完成后的回答

### 4.3 五个能力开关

聊天请求体支持：

- `useTools`
- `useWebSearch`
- `useStructuredOutput`
- `useHybridRetrieval`
- `useImageVision`

后端统一归一化逻辑：

- `useWebSearch=true` 时，`useTools` 也会视为开启
- `useHybridRetrieval` 默认开启
- `useImageVision` 默认开启

## 5. 工具调用实现

工具调用定义位于：

```text
chatWorkspaceService.js -> buildToolDefinitions()
```

当前内置工具：

- `search_knowledge`
  - 检索本地知识库和已索引附件
- `web_search`
  - 搜索公开网页

执行闭环：

1. 首轮模型请求允许返回 `tool_calls`
2. 后端解析 `tool_calls`
3. 本地执行工具函数
4. 将工具结果作为 `role: "tool"` 消息回填
5. 再次请求模型生成最终答案

这让聊天系统从“纯文本问答”升级为“可调用工具的问答代理”。

## 6. 结构化输出实现

当 `useStructuredOutput=true` 时：

- 系统提示词中会加入结构化输出要求
- 请求体中附加：

```json
{
  "response_format": {
    "type": "json_object"
  }
}
```

作用：

- 让模型返回 JSON 对象
- 便于后续学习计划、题目、资源等结构化消费

当前仍是通用 JSON 约束，还没有针对每个工作流定义更细的 schema。

## 7. 网页搜索与 URL 导入

### 7.1 网页搜索

实现文件：

```text
1/server/src/services/webToolService.js
```

当前实现方式：

- 使用 DuckDuckGo HTML 结果页
- 抓取搜索结果标题、链接、摘要
- 可在聊天中作为上下文或工具结果回填给模型

### 7.2 URL 导入

接口：

```text
POST /api/knowledge/import-url
```

流程：

1. 校验 URL
2. 抓取网页内容
3. 提取 `<title>` 与正文纯文本
4. 生成知识文档
5. 文本切片
6. 写入 `knowledge_documents`
7. 写入 `document_chunks`
8. 写入 `document_chunks_fts`

当前限制：

- 以正文纯文本为主
- 不保留复杂样式、表格布局和图片结构

## 8. 混合检索实现

实现文件：

```text
1/server/src/services/ragService.js
```

### 8.1 当前检索模式

- `keyword`
  - 基于 SQLite FTS5 关键词召回
- `vector`
  - 基于本地轻量向量相似度召回
- `hybrid`
  - 合并关键词召回和向量召回，再做 rerank

### 8.2 轻量向量实现

当前没有依赖外部 embedding API，而是采用本地哈希向量：

- 向量维度：`128`
- 将文本 token 哈希到固定维度桶
- 做归一化后用于余弦相似度计算

优点：

- 无需额外模型服务
- 零外部依赖
- 足够支撑本地语义增强检索

缺点：

- 语义能力弱于真实 embedding 模型

### 8.3 rerank

重排综合了三部分分数：

- 关键词分数
- 向量分数
- 词面重叠分数

当前权重大致为：

- keyword：`0.36`
- vector：`0.44`
- overlap：`0.2`

## 9. 图片识别输入实现

目标：

- 让上传图片时，不只保存文件名，而是尽量按多模态方式让模型真正“看图”

实现要点：

1. 图片附件上传后保存到 `chat_attachments.content_data`
2. `useImageVision=true` 时筛选图片附件
3. 组装为多模态消息：

```json
{
  "type": "image_url",
  "image_url": {
    "url": "data:image/png;base64,..."
  }
}
```

4. 与文本一起发给模型

限制：

- 只有模型本身支持视觉时才会生效
- 当前前端主要解决“把图片送进去”，不负责 OCR 专项流程

## 10. 知识库实现

核心文件：

- `knowledgeBaseService.js`
- `documentParserService.js`
- `chunkingService.js`

支持来源：

- 文本文件
- Markdown
- PDF
- DOCX
- URL 网页正文

处理流程：

1. 解析内容
2. 切片
3. 写入 `knowledge_documents`
4. 写入 `document_chunks`
5. 写入 FTS 索引
6. 生成轻量向量并存入 `embedding_json`

## 11. 数据库设计

核心表：

- `model_configs`
- `knowledge_bases`
- `knowledge_documents`
- `document_chunks`
- `chat_sessions`
- `chat_messages`
- `chat_attachments`
- `agents`
- `learning_plans`
- `generated_resources`
- `questions`
- `answers`
- `mistakes`
- `connection_history`

本次新增的关键字段：

- `chat_attachments.content_data`
  - 保存视觉附件原始 data URL
- `document_chunks.embedding_json`
  - 保存每个切片的本地向量

全文检索表：

- `document_chunks_fts`

## 12. 运行时配置

运行时配置默认存放于：

```text
文档\agent API\runtime-config.json
```

保存逻辑：

- 前端调用 `/api/models/current`
- 后端写入运行时配置
- Windows 下 API Key 做保护存储
- 读取时返回脱敏后的设置和当前模型信息

## 13. 前端输入区布局调整记录

最近针对聊天输入区做了几轮实际界面适配：

- 将五个能力开关移动到输入区内部底部工具带
- 将状态提示移到输入框下方
- 缩小输入框默认高度
- 将附件按钮改为 `+`
- 通过增加底部留白让输入区整体上移，避免被任务栏或窗口底边遮挡

这些调整主要集中在：

```text
1/renderer/src/components/workspace/ComposerPanel.vue
```

## 14. 测试覆盖

集成测试文件：

```text
1/server/test/app.integration.test.js
```

当前覆盖点：

- 无运行时配置时的 mock 聊天回复
- 流式聊天事件返回
- 知识库导入与检索
- 图片附件标记为视觉消息
- 运行时配置保存与读取
- 空消息校验

## 15. 当前限制

- 联网搜索仍是轻量实现，搜索质量依赖 DuckDuckGo HTML 结果页
- URL 导入只保留正文纯文本，不做复杂页面结构还原
- 本地轻量向量不是严格意义上的高质量 embedding
- 结构化输出目前只做通用 JSON 约束，还没有业务级 schema
- 图片识别能力取决于实际接入的模型是否支持视觉
- 多智能体当前仍偏“角色切换”，不是完整的 planner/executor/reviewer 编排

## 16. 后续演进建议

1. 为学习工作流补充固定 schema，进一步提升结构化结果稳定性
2. 把轻量向量升级为真实 embedding 服务，支持更强语义检索
3. 为 URL 导入补充更稳的正文提取和去噪策略
4. 为视觉输入补充前端缩略图和失败回退提示
5. 将工具系统扩展为更多函数，例如网页抓取、知识库摘要、计划生成器等
