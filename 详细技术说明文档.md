# AI 学习多智能体工作台详细技术说明文档

## 1. 文档目标

本文档说明 `1` 目录中项目的技术选型、目录结构、模块职责、数据流、聊天工作区实现方式和后续扩展建议，供开发交接、答辩说明和后续迭代使用。

## 2. 项目概述

项目名称：AI 学习多智能体工作台  
落地目录：[`1`](./1)

项目定位：

- 一个桌面端 AI 学习系统
- 支持模型配置、知识库管理、智能体编排
- 以聊天工作区作为默认入口
- 通过多智能体协同完成学习支持任务

当前系统围绕以下主链路运行：

1. 用户进入聊天工作区
2. 系统加载默认模型、智能体和历史会话
3. 用户输入指令或上传附件
4. 后端写入会话、消息、附件
5. 检索智能体和规划智能体返回协同状态
6. 用户继续进入资源生成、练习与反馈页面

## 3. 技术选型

### 3.1 前端

- Vue 3
- Vite
- Vue Router
- Pinia
- Element Plus

选型原因：

- 适合快速构建桌面端业务界面
- 路由、状态、UI 生态成熟
- 开发效率高，适合快速演进

### 3.2 桌面端

- Electron

选型原因：

- 与 JS 技术栈一致
- 便于未来接入文件系统、下载、系统通知等桌面能力

### 3.3 后端

- Node.js
- Express

选型原因：

- 便于在本地提供 API 服务
- 分层简单，适合当前业务规模

### 3.4 数据层

- SQLite
- better-sqlite3

选型原因：

- 本地单文件数据库
- 适合 Electron 桌面应用
- 后续可扩展向量检索能力

## 4. 当前实现范围

当前项目不是纯静态原型，而是已经具备真实前后端结构的可运行版本，包含：

- Electron 主进程和 preload
- Vite + Vue 前端
- Express 本地 API
- SQLite 建表与种子数据
- 默认聊天工作区
- 会话、消息、附件的本地持久化
- 模型、知识库、智能体、学习工作流页面和接口

当前尚未实现的部分：

- 真实第三方 LLM 调用
- 真实附件落盘与解析
- 真实向量检索和 RAG
- 用户体系和鉴权

## 5. 目录结构

```text
1/
├─ electron/
│  ├─ main.cjs
│  └─ preload.cjs
├─ renderer/
│  ├─ index.html
│  └─ src/
│     ├─ App.vue
│     ├─ components/
│     ├─ layouts/
│     ├─ router/
│     ├─ services/
│     ├─ stores/
│     ├─ styles/
│     └─ views/
├─ server/
│  ├─ data/
│  └─ src/
│     ├─ app.js
│     ├─ index.js
│     ├─ config/
│     ├─ data/
│     ├─ db/
│     ├─ routes/
│     ├─ services/
│     └─ utils/
├─ dist/
├─ package.json
└─ vite.config.js
```

## 6. 架构设计

### 6.1 总体架构

```text
Electron Shell
   ↓
Vue Renderer
   ↓
Express Local API
   ↓
SQLite / 未来的 LLM Provider / 未来的向量检索层
```

### 6.2 Electron 层

职责：

- 创建窗口
- 开发模式加载 Vite 地址
- 生产模式加载打包后的前端页面
- 通过 preload 暴露基础平台信息

当前文件：

- [1/electron/main.cjs](/E:/基于Deepseek的智能体学习软件/1/electron/main.cjs)
- [1/electron/preload.cjs](/E:/基于Deepseek的智能体学习软件/1/electron/preload.cjs)

### 6.3 Renderer 层

职责：

- 承载聊天、配置、知识库、智能体、资源生成、测评等页面
- 通过 Axios 调用本地 API
- 展示消息、附件、协同状态和工作流结果

关键点：

- `renderer/index.html` 已改为真正的 Vite 入口
- 默认路由为聊天工作区，而不是旧的静态 HTML 原型

### 6.4 Local API 层

职责：

- 统一承接前端请求
- 管理 SQLite 读写
- 组织会话、消息、知识库、模型、智能体和学习流程逻辑
- 为未来接入真实 LLM 和 RAG 预留位置

### 6.5 Data 层

职责：

- 存储模型配置、知识库信息、智能体、资源、练习、反馈
- 存储聊天会话、消息和附件元数据

数据库文件路径：

```text
1/server/data/app.db
```

## 7. 前端模块设计

### 7.1 主布局

文件：`renderer/src/layouts/MainLayout.vue`

作用：

- 提供统一侧边导航
- 显示平台和 API 地址
- 承载所有业务页面

### 7.2 路由

文件：`renderer/src/router/index.js`

当前主要路由：

- `/`：`ChatWorkspaceView.vue`
- `/dashboard`：`DashboardView.vue`
- `/models`：`ModelsView.vue`
- `/knowledge`：`KnowledgeView.vue`
- `/agents`：`AgentsView.vue`
- `/learning`：`LearningView.vue`
- `/practice`：`PracticeView.vue`
- `/analytics`：`AnalyticsView.vue`

### 7.3 聊天工作区

文件：`renderer/src/views/ChatWorkspaceView.vue`

能力：

- 加载当前默认模型
- 展示会话列表
- 加载活动会话详情
- 展示消息流
- 展示多智能体协同状态
- 上传附件
- 发送消息

前端交互流程：

1. 页面挂载后请求 `/api/chat/workspace`
2. 读取模型、智能体、会话和活动会话
3. 上传附件时调用 `/api/chat/attachments`
4. 发送消息时调用 `/api/chat/messages`
5. 发送完成后重新刷新工作区状态

### 7.4 其他业务页面

当前还包括：

- 总览页
- 模型配置页
- 知识库管理页
- 智能体编排页
- 学习资源生成页
- 练习与测评页
- 数据可视化页

这些页面已经可以与后端示例接口联调。

## 8. 后端模块设计

### 8.1 路由层

位于 `server/src/routes/`，当前包括：

- `health.js`
- `dashboard.js`
- `models.js`
- `knowledge.js`
- `agents.js`
- `workflows.js`
- `chat.js`

职责：

- 接收请求
- 调用服务层
- 返回 JSON

### 8.2 服务层

位于 `server/src/services/`，当前关键服务包括：

- `modelConfigService.js`
- `knowledgeBaseService.js`
- `agentService.js`
- `learningWorkflowService.js`
- `chatWorkspaceService.js`
- `bootstrapService.js`

职责：

- 处理业务逻辑
- 执行数据库读写
- 生成示例响应

### 8.3 初始化逻辑

启动时执行：

1. `bootstrapDatabase()` 读取 `schema.sql` 自动建表
2. `seedInitialData()` 写入模型、知识库、智能体种子数据
3. 自动补齐聊天工作区所需的 `retrieval` 和 `planning` 智能体

## 9. 聊天工作区后端设计

### 9.1 新增数据表

当前聊天域新增三张表：

- `chat_sessions`
- `chat_messages`
- `chat_attachments`

用途：

- `chat_sessions`：会话元信息
- `chat_messages`：消息内容和智能体状态快照
- `chat_attachments`：附件元数据和文本摘录

### 9.2 核心服务

文件：`server/src/services/chatWorkspaceService.js`

当前实现能力：

- 获取当前默认模型
- 确保工作区核心智能体存在
- 自动创建活动会话
- 自动插入欢迎消息
- 获取会话列表和会话详情
- 附件登记
- 发送消息并生成助手回复
- 构造智能体协同状态

### 9.3 当前消息生成方式

当前聊天回复不是直连真实模型，而是服务层基于以下信息生成本地响应：

- 当前默认模型
- 当前消息内容
- 已选附件
- 已有知识库列表
- 最近会话上下文

这让页面、数据库和 API 链路已经完整，但推理部分仍然是示例实现。

## 10. 数据库设计

当前已落地表：

- `users`
- `model_configs`
- `knowledge_bases`
- `documents`
- `agents`
- `learning_plans`
- `generated_resources`
- `questions`
- `answers`
- `mistakes`
- `chat_sessions`
- `chat_messages`
- `chat_attachments`

### 10.1 关键关系

- 一个知识库对应多个文档
- 一个会话对应多条消息
- 一个会话对应多个附件
- 一条用户消息可以绑定多个附件
- 一条助手消息包含一组智能体状态快照

## 11. 已验证内容

当前已经验证：

- 前端可成功构建
- 后端可成功初始化应用
- 聊天工作区状态接口可返回模型、会话、消息和智能体

说明：

- 当前页面和接口结构已经可用于演示和继续开发
- 真实 AI 推理与真实知识库处理仍需下一阶段补齐

## 12. 后续演进建议

### 12.1 真实模型接入

建议新增 `providers/` 目录，统一封装：

- OpenAI-Compatible Provider
- DeepSeek Provider
- Qwen Provider
- Ollama Provider

### 12.2 真实附件处理

建议补齐：

1. 文件落盘
2. 文档解析
3. 文本清洗
4. 分块切片
5. 附件与知识库关联

### 12.3 检索增强

建议补齐：

1. 向量化
2. 向量索引
3. 检索召回
4. 重排序
5. 引用片段拼接到聊天上下文

### 12.4 多智能体深化

在现有检索智能体、规划智能体基础上，可以继续增加：

- 资源生成智能体
- 练习生成智能体
- 反馈评价智能体
- 学习画像智能体

## 13. 当前限制

- 未接入真实第三方模型调用
- 附件未真实保存到文件系统
- 知识库检索仍是示例逻辑
- 还没有导出、鉴权、日志和自动更新能力

## 14. 总结

当前项目已经从“静态原型”升级为“可运行的桌面端前后端应用骨架”，尤其是聊天工作区已经具备真实页面、真实 API、真实数据库写入和多智能体状态展示能力。后续工作的重点不再是页面搭骨架，而是把真实模型推理、文档处理和检索链路逐步接入现有架构。
