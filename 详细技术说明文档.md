# AI 学习多智能体工作台详细技术说明文档

## 1. 文档目标

本文档说明 `1` 目录中项目的技术选型、目录结构、模块职责、数据流、聊天工作区实现方式、RAG 检索增强管线以及后续扩展建议，供开发交接、答辩说明和后续迭代使用。

## 2. 项目概述

项目名称：AI 学习多智能体工作台
落地目录：[`1`](./1)

项目定位：

- 一个桌面端 AI 学习系统
- 支持模型配置、知识库管理、智能体编排
- 以聊天工作区作为默认入口
- 通过多智能体协同 + RAG 检索增强完成学习支持任务

当前系统围绕以下主链路运行：

1. 用户进入聊天工作区
2. 系统加载默认模型、智能体和历史会话
3. 用户上传 PDF/DOCX/MD 等文档 → 服务端自动解析纯文本 → 切块 → FTS5 索引
4. 用户输入指令或上传附件
5. 后端写入会话、消息、附件
6. RAG 检索召回相关文档片段 → 注入 LLM 上下文
7. LLM 生成带引用的回答 → 前端展示"参考来源"卡片
8. 用户继续进入知识库、智能体市场、设置等模块

## 3. 技术选型

### 3.1 前端

- Vue 3（Composition API）
- Vite 6
- Vue Router 4（Hash 模式）
- Pinia 2
- Element Plus 2

### 3.2 桌面端

- Electron 35

### 3.3 后端

- Node.js
- Express 4
- better-sqlite3 11（含 FTS5 全文搜索）

### 3.4 文档解析

- pdf-parse — PDF 文本提取（基于 Mozilla PDF.js）
- mammoth — DOCX 转纯文本

### 3.5 文本检索

- SQLite FTS5 — 全文索引 + BM25 相关性排序
- 自定义 `chunkingService` — 段落感知滑窗切块

## 4. 当前实现范围

当前项目是已具备真实前后端结构的可运行版本，包含：

- Electron 主进程和 preload
- Vite + Vue 前端（单页应用）
- Express 本地 API
- SQLite 建表与种子数据 + FTS5 全文索引
- 默认聊天工作区（含历史会话管理）
- 会话、消息、附件的本地持久化
- **PDF / DOCX 文档解析与文本提取**
- **自动文档切块与 FTS5 全文索引**
- **RAG 检索召回 + 引用展示**
- **外部 LLM 接入（DeepSeek / OpenAI-Compatible）**
- 模型配置、知识库管理、智能体市场、学习工作流

当前尚未实现的部分：

- 向量语义检索（当前为 FTS5 关键词检索）
- 流式响应（SSE）
- 用户体系和鉴权
- PPT / XLSX 等更多格式解析

## 5. 目录结构

```text
1/
├─ electron/
│  ├─ main.cjs              Electron 主进程（窗口、生命周期）
│  └─ preload.cjs           桌面能力暴露
├─ renderer/
│  ├─ index.html            Vite 入口页
│  └─ src/
│     ├─ App.vue            根组件（只含 <router-view />）
│     ├─ main.js            挂载入口
│     ├─ components/
│     │  ├─ SectionBlock.vue         通用卡片区组件
│     │  ├─ StatusCard.vue           通用状态卡片
│     │  └─ workspace/
│     │     ├─ ChatHistoryPanel.vue   历史会话列表（含重命名/删除）
│     │     ├─ ChatPane.vue           消息流 + 引用展示
│     │     ├─ ComposerPanel.vue      消息输入区
│     │     ├─ KnowledgeList.vue      知识库文档列表
│     │     ├─ SettingsForm.vue       模型配置表单
│     │     └─ StatusPanel.vue        右侧状态栏
│     ├─ composables/
│     │  └─ useRuntimeSubscription.js SSE 监听运行时配置
│     ├─ router/
│     │  └─ index.js         路由配置（'/'→StudyWorkspaceView）
│     ├─ services/
│     │  └─ api.js           Axios API 封装（含 RAG 相关调用）
│     ├─ stores/
│     │  ├─ app.js           应用级状态
│     │  └─ runtime.js       运行时配置状态
│     ├─ styles/
│     │  └─ theme.css        全局主题变量
│     ├─ utils/
│     │  └─ workspaceFormatters.js   Markdown 渲染、引用渲染、Base64 读取等
│     └─ views/
│        ├─ StudyWorkspaceView.vue   主入口（单页应用，侧边导航）
│        └─ ...（其他旧视图文件，未被路由引用）
├─ server/
│  ├─ data/
│  │  ├─ .gitkeep
│  │  ├─ app.db
│  │  ├─ app.db-wal
│  │  └─ app.db-shm
│  └─ src/
│     ├─ app.js              Express 应用工厂
│     ├─ index.js            服务端入口
│     ├─ config/
│     │  ├─ database.js      数据库初始化 + 自动迁移
│     │  └─ env.js           环境变量
│     ├─ data/
│     │  └─ seedData.js      种子数据
│     ├─ db/
│     │  └─ schema.sql       建表脚本（含 FTS5）
│     ├─ routes/
│     │  ├─ index.js          路由注册
│     │  ├─ chat.js           聊天工作区路由
│     │  ├─ knowledge.js      知识库路由
│     │  ├─ models.js         模型配置路由
│     │  ├─ agents.js         智能体路由
│     │  ├─ dashboard.js      总览路由
│     │  ├─ health.js         健康检查
│     │  └─ workflows.js      学习工作流路由
│     ├─ services/
│     │  ├─ chatWorkspaceService.js      聊天工作区核心逻辑
│     │  ├─ documentParserService.js     PDF/DOCX/MD 文本解析
│     │  ├─ chunkingService.js          文本切块
│     │  ├─ ragService.js               FTS5 索引/检索/引用构建
│     │  ├─ knowledgeBaseService.js      知识库管理
│     │  ├─ agentService.js             智能体管理
│     │  ├─ modelConfigService.js       模型配置
│     │  ├─ runtimeConfigService.js     运行时配置
│     │  ├─ dashboardService.js         总览数据
│     │  ├─ learningWorkflowService.js  学习工作流
│     │  ├─ bootstrapService.js         启动种子数据
│     │  └─ ...
│     └─ utils/
│        ├─ id.js               ID 生成器
│        ├─ requestValidation.js 请求校验
│        └─ schemaValidation.js  Schema 校验
├─ dist/
├─ package.json
└─ vite.config.js
```

## 6. 架构设计

### 6.1 总体架构

```text
Electron Shell
     ↓
Vue Renderer（单页应用，侧边导航切换模块）
     ↓
Express Local API（路由 → 服务 → 数据库）
     ↓
┌──────────┬──────────────┬──────────────────┐
│  SQLite  │  FTS5 全文   │  LLM Provider    │
│  持久化  │  检索 + RAG  │  (DeepSeek/Qwen) │
└──────────┴──────────────┴──────────────────┘
```

### 6.2 RAG 检索增强管线

```
上传 PDF/DOCX
  → 前端 FileReader.readAsDataURL() → Base64 Data URL
  → POST /api/chat/attachments { contentText: "data:application/pdf;base64,..." }
  → documentParserService.parseDocumentFromUpload()
    → pdf-parse (PDF) / mammoth (DOCX) → 纯文本
  → chunkingService.chunkText(text, { chunkSize: 500, overlap: 100 })
    → 按段落边界切分，超限按句/词切分
  → ragService.indexChunks("chat_attachment", id, name, chunks)
    → INSERT document_chunks + INSERT document_chunks_fts

发送消息
  → ragService.searchChunks(userQuery, { topK: 5 })
    → FTS5 MATCH + rank 排序
  → ragService.buildCitationPrompt(results)
    → 构建 [REF:1]...[REF:5] 提示块 + citationMeta 元数据
  → chatWorkspaceService.buildModelMessages({ ragContext })
    → 将引用提示块追加到 system prompt
  → requestModelReply() → LLM API

返回前端
  → assistantMessage.citations = [{ refId, sourceName, chunkIndex, snippet }]
  → ChatPane.vue 渲染"参考来源"卡片
```

### 6.3 Electron 层

职责：

- 创建窗口
- 开发模式加载 Vite 地址
- 生产模式加载打包后的前端页面
- 通过 preload 暴露基础平台信息

### 6.4 Renderer 层

当前路由结构：

- `'/'` → `StudyWorkspaceView.vue`（唯一路由，单页应用）

`StudyWorkspaceView.vue` 内部通过 `currentPage` ref 切换四个模块：

- `chat` — 聊天工作区（ChatHistoryPanel + ChatPane + ComposerPanel + StatusPanel）
- `kb` — 知识库管理（KnowledgeList）
- `market` — 智能体市场
- `settings` — 模型配置（SettingsForm）

关键点：

- `renderer/index.html` 为 Vite 入口
- 不再使用 `layouts/` 目录（已删除）
- 聊天组件全部在 `components/workspace/` 下
- 所有 API 调用集中在 `services/api.js`

### 6.5 Local API 层

路由层：`server/src/routes/` — 接收请求、参数校验、调用服务层
服务层：`server/src/services/` — 业务逻辑、数据库读写、外部 API 调用

### 6.6 Data 层

数据库文件：`1/server/data/app.db`

核心表关系：

- 一个知识库 → 多个知识文档 → 多个文档切块
- 一个聊天会话 → 多条消息 → 含 citations_json
- 一个聊天会话 → 多个附件 → 多个附件切块
- 切块统一存储在 `document_chunks`，通过 `source_type` + `source_id` 区分来源

## 7. 聊天工作区后端设计

### 7.1 核心服务

文件：`server/src/services/chatWorkspaceService.js`

实现能力：

- 获取当前默认模型
- 确保工作区核心智能体存在（retrieval + planning）
- 自动创建活动会话
- 自动插入欢迎消息（含模型、智能体、知识库状态）
- 获取会话列表和会话详情
- 附件上传 + 解析 + 切块 + FTS5 索引
- 发送消息 + RAG 检索 + LLM 请求 + 引用返回
- 构造智能体协同状态
- 会话重命名、删除、清空（含切块清理）

### 7.2 消息生成方式

当前聊天回复支持两种模式：

1. **外部模型**：如果设置中配置了 Base URL + API Key + Model ID，则调用 `/chat/completions` 接口，获取真实 AI 回复
2. **本地演示**：未配置外部模型时，基于当前模型名、智能体、知识库信息生成本地演示回复

两种模式下 RAG 检索都会执行——外部模型会通过 system prompt 注入相关文档片段，本地演示回复也会在文本中体现。

### 7.3 返回消息结构变化

相比早期版本，`sendMessage()` 的 `assistantMessage` 新增了：

- `citations` 数组 — RAG 检索引用的文档片段列表
- `source` 字段 — `"api"`（外部模型）或 `"mock"`（本地演示）

同时 `chat_messages` 表新增了 `citations_json` 列用于持久化引用数据。

## 8. 数据库设计

### 8.1 当前所有表

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `users` | 用户信息 | id, name, current_goal |
| `model_configs` | 模型配置 | id, name, provider, base_url, api_key_masked, model_id, is_default |
| `knowledge_bases` | 知识库 | id, name, category, vector_store, document_count |
| `knowledge_documents` | 知识文档 | id, knowledge_base_id, name, source_type, chunk_count, summary |
| `documents` | 文档记录 | id, knowledge_base_id, title, source_type, status |
| `agents` | 智能体 | id, name, role, model_binding, prompt_template, knowledge_scope |
| `learning_plans` | 学习计划 | id, user_id, goal, difficulty, stages_json |
| `generated_resources` | 生成资源 | id, knowledge_base_id, resource_type, title, content |
| `questions` | 题目 | id, knowledge_base_id, question_type, prompt, answer, analysis |
| `answers` | 答案 | id, question_id, answer_text, score, feedback |
| `mistakes` | 错题 | id, answer_id, cause, recommendation |
| `chat_sessions` | 聊天会话 | id, title, model_config_id, status, updated_at |
| `chat_messages` | 聊天消息 | id, session_id, role, content, citations_json, agent_statuses_json |
| `chat_attachments` | 聊天附件 | id, session_id, name, mime_type, content_excerpt |
| `document_chunks` | RAG 切块 | id, source_type, source_id, source_name, chunk_index, content |
| `document_chunks_fts` | FTS5 全文索引 | source_name, content（虚拟表，通过 rowid 关联 document_chunks） |

### 8.2 关键关系

- 一个知识库对应多个知识文档，一个知识文档对应多个切块
- 一个聊天会话对应多条消息，一个会话对应多个附件
- 一个附件对应多个切块（source_type="chat_attachment"）
- 一个知识文档对应多个切块（source_type="knowledge_document"）
- 一条助手消息包含一组引用元数据（citations_json）

## 9. 已验证内容

当前已经验证：

- 前端可成功构建（Vite build 通过）
- 后端可成功初始化应用（Express + SQLite + FTS5）
- 聊天工作区状态接口可返回模型、会话、消息和智能体
- 文档解析可正常提取 PDF/DOCX 文本
- 文本切块和 FTS5 索引正常工作
- RAG 检索可返回相关切块
- 引用数据可通过 API 返回并在前端渲染

## 10. 后续演进建议

### 10.1 向量检索

当前 FTS5 关键词检索在精确匹配场景下表现尚可，但对语义相近的表达召回有限。建议引入 sqlite-vec 扩展或外部 Embedding API 实现语义向量检索。

### 10.2 流式响应

当前聊天回复为一次性返回，建议增加 SSE 流式响应支持，提升交互体验。

### 10.3 更多文档格式

当前支持 PDF、DOCX、MD、TXT，建议扩展支持 PPT、XLSX、EPUB 等格式。

### 10.4 多智能体深化

在现有检索智能体、规划智能体基础上，可以继续增加：

- 资源生成智能体
- 练习生成智能体
- 反馈评价智能体
- 学习画像智能体

### 10.5 工程化

- 添加请求日志和错误追踪
- 添加应用打包（electron-builder）
- 添加自动更新能力

## 11. 当前限制

- 检索为 FTS5 关键词匹配，非语义向量检索
- 不支持流式响应
- 附件上限 15MB（受 Express JSON body 限制）

## 12. 总结

当前项目已经从"静态原型"升级为"可运行的桌面端前后端应用"，尤其是聊天工作区已经具备真实页面、真实 API、真实数据库写入、多智能体状态展示和 **RAG 检索增强闭环**。后续工作的重点是升级检索精度（语义向量）、增加流式响应、扩展文档格式支持和打包发布。
